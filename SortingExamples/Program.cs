using System;

namespace SortingExamples
{
    class Program
    {
        static void Main(string[] args)
        {
           // Примери за сортиране:
           // var thenByResult = studentList.OrderBy(s => s.StudentName).ThenBy(s => s.Age);
           // keyMaterials = keyMaterials.OrderByDescending(kvp => kvp.Value)
           //.ThenBy(kvp => kvp.Key)
           //.ToDictionary(a => a.Key, b => b.Value);
           // foreach (var currentTrack in tracks.OrderByDescending(x => x.Brand))
           //     foreach (var currentBox in boxes.OrderByDescending(x => x.PriceForAbox))
           //         foreach (var currentCar in cars.OrderBy(x => x.Brand))
        }
    }
}
